\documentclass[preview]{standalone}
\usepackage{tikz}
\begin{document}
\usetikzlibrary{graphs}
\usetikzlibrary{graphdrawing}
\usegdlibrary{trees}

\begin{tikzpicture}[
	tree layout, component sep=5em,
	subgraph nodes={draw=red, rounded corners, inner sep=1.5em},
	caret/.style={graphs/as=, inner sep=0, minimum size=0},
	leaf/.style={circle, inner sep=1pt, fill},
]
[component direction=right]
\graph [nodes=caret] {
domain [name=domain] // [tree layout, grow=down, component direction=right, component sep=2.5em] {
	dx1 [root]
		-- dx1a1 [label=below:1, leaf],
		-- dx1a2 [label=below:2, leaf],
	dx2 [label=below:3, leaf, root],
	dx3 [label=below:4, leaf, root],
};

range [name=range] // [tree layout, grow=down, component direction=right, component sep=2.5em] {
	rx1 [root]
		-- rx1a1 [label=below:5, leaf],
	rx1 [root]
		-- rx1a2
		-- rx1a2a1 [label=below:9, leaf],
	rx1a2
		-- rx1a2a2 [label=below:3, leaf],
};

domain -> range;

};
\end{tikzpicture}
\end{document}
